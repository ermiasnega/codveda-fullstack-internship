Level 1 – Task 1: Development Environment Setup

Introduction

For this task, I set up my development environment using GitHub Codespaces. I will use this environment to develop and test the full-stack applications required for the Codveda internship.

Tools I Set Up

The main tools I am using are:

- GitHub
- GitHub Codespaces
- Node.js
- npm
- Git
- MongoDB Atlas

Environment Verification

I checked that the main development tools were installed correctly.

Node.js

I used the following command:

node --version

The installed version is:

v24.14.0

npm

I checked npm using:

npm --version

The installed version is:

11.9.0

Git

I checked Git using:

git --version

The installed version is:

git version 2.53.0

All three tools are working correctly in my GitHub Codespaces environment.

GitHub Repository

I created a GitHub repository for my internship work:

Repository: "codveda-fullstack-internship"

The repository is connected to my Codespaces environment through the "origin" remote.

I checked this using:

git remote -v

The repository is using the "main" branch.

Git Commands

During this task, I practiced some basic Git commands that I will use throughout the internship:

git status
git add .
git commit -m "message"
git push
git pull
git remote -v

These commands allow me to check my changes, save my work, and keep my local Codespaces project synchronized with GitHub.

Database

I will use MongoDB Atlas as the database for the project. It will be connected when I start building the REST API in the next task.

Result

The development environment is now ready. Node.js, npm, and Git are installed and working, and my GitHub repository is connected to GitHub Codespaces.